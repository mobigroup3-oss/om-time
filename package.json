{
  "name": "om-time",
  "private": true,
  "description": "OM-Time — статический сайт (React+Babel из CDN) + serverless-функции api/ на Vercel. Build Command пустой; зависимость нужна только функциям.",
  "dependencies": {
    "@vercel/blob": "^0.27.0",
    "@vercel/postgres": "^0.10.0"
  }
}
